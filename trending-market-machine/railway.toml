[build]
  dockerfilePath = "trending-market-machine/Dockerfile"

[deploy]
  startCommand = "node dist/cli.js watch"
  healthcheckPath = "/health"
  healthcheckTimeout = 30
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
