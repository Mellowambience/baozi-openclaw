services:
  - type: web
    name: baozi-discord-bot
    runtime: node
    buildCommand: cd discord-bot && npm install && npm run build
    startCommand: cd discord-bot && npm start
    envVars:
      - key: DISCORD_BOT_TOKEN
        sync: false
      - key: DISCORD_CLIENT_ID
        sync: false
