[build]
  builder = "NIXPACKS"

[[services]]
  name = "agent-arena"
  source = "agent-arena"

  [services.deploy]
    startCommand = "npm start"
    restartPolicyType = "ON_FAILURE"
    restartPolicyMaxRetries = 3
